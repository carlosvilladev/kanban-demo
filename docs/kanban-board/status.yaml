feature: kanban-board
status: in-review
profile: code
action_type: ADD
compact: true
persona: balanced
created_at: '2026-06-28'
in_review_at: '2026-06-29'
dependencies:
  blocking: []
  blocks:
    - drag-and-drop
    - persistence-seed
notes: >-
  Core/foundation feature. Defines the shared Task/Column/BoardState type model
  and the single-source-of-truth board store (BoardProvider/useBoard) consumed
  by persistence-seed (supplies initialState + auto-save) and drag-and-drop
  (adds an atomic MOVE_TASK action). All 4 tasks implemented. 121 tests pass
  (52 original persistence-seed tests + 69 new kanban-board tests). Build, lint,
  and tsc all clean. App.tsx wired to loadInitialBoard() + useAutoPersist.
